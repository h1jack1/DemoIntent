<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_capitalized"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.algonquincollege.mad9132.demointent.CapitalizedTextActivity" >

    <TextView
        android:id="@+id/label_capitalized"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/capitalized_text_label" />

    <ScrollView
        android:id="@+id/scroll_capitalized"
        android:layout_width="fill_parent"
        android:layout_height="360dp"
        android:layout_below="@+id/label_capitalized"
        android:background="@drawable/scrollviewborder" >

        <EditText
            android:id="@+id/text_capitalized"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:hint="@string/capitalized_text_hint"
            android:inputType="textMultiLine" >

            <requestFocus />
        </EditText>
    </ScrollView>

    <Button
        android:id="@+id/button_capitalized"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/scroll_capitalized"
        android:layout_alignParentBottom="true"
        android:text="@string/clear" />

</RelativeLayout>
